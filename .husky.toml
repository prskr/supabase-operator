[hooks]

# git hook pre commit
pre-commit = [
    "bazel run @rules_go//go -- mod tidy",
    "husky lint-staged",
    # "golangci-lint run",
]

# git hook commit msg
commit-msg = [
    "husky lint-commit",
]

# list staged files do some pre-process and git add
[lint-staged]
"*.go" = [
    "goimports -l -w",
    "gofumpt -l -w",
]
