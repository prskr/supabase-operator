common --experimental_isolated_extension_usages --incompatible_enable_proto_toolchain_resolution --@protobuf//bazel/flags:prefer_prebuilt_protoc
# run --run_in_cwd

try-import %workspace%/user.bazelrc
